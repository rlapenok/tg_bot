syntax = "proto3";
package api;

message GetKeysResp{
    uint32 len=1;
}


enum Request{
    GetKeys=0;
}
message GlobalRequest{
    Request req=1;
}


service Router{
        rpc GetKeys(GlobalRequest) returns (stream GetKeysResp);
}